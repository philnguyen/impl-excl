#lang info

(define collection "impl-excl")

(define deps '("base"
               "typed-racket-lib"
               "typed-racket-more"))

(define pkg-desc "Compute procedures for fast checking of implications and exclusions between simple predicates")

(define pkg-authors '(pcn))
